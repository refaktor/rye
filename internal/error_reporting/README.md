# Error reporting tests

I am using scripts here to test the runtime and the parser (loader) to make sure error reporting works as it should.


detect if builtin retuns nil and report error in evaluator

