# Probing

These are internal tests with which we are developintg the runtime and the loader (parser).

Tests of builtin functions can be found in info/ folder. `cd ../info`. You can then run them by `rye . test`.

--

