this is a submodule of rye project meant for embedding Rye language into other go projects, it's a separate submodule so when user adds rye/embed2 into their project it doesn't pull all the builtins (now separated into batteries) just the language level builtisn. But it still has some dependencies like go-landlock fsnotify , etc .. .that don't belong into pure embedede lang environment because all IO and affectors should be provided by the hosts system. Analyze the deps in go.mod and figure out why they are dependencies in rye/embed2 still , do we need them, could we structure code differently so they aren't included here wheer we need no IO and no separate binary , even repl
